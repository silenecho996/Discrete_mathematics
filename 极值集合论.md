# 极值集合论核心内容整理

## 目录
1. 基本概念与定义
2. 向日葵引理
3. Erdős–Ko–Rado 定理
4. Sperner 定理
5. LYM 不等式
---
## 1. 基本概念与定义
### 1.1 基集 (Ground Set)
**定义**：通常记作 $[n] = \{1, 2, \dots, n\}$，是讨论中所有元素的全集。几乎所有讨论的集合都是 $[n]$ 的子集。
**例子**：$[3] = \{1, 2, 3\}$。

---
### 1.2 子集 (Subset)
**定义**：由 $[n]$ 中一些元素组成的集合。
**例子**：$\{1, 3\}$ 是 $[3]$ 的一个子集。

---
### 1.3 集合族 / 族 (Set System / Family)
**定义**：$\mathcal{F} \subseteq 2^{[n]}$，即以 $[n]$ 的一些子集为元素构成的集合。通俗地说，就是“集合的集合”。
这里 $2^{[n]}$ 表示 $[n]$ 的所有子集构成的集合（幂集）。
**例子**：
$$\mathcal{F} = \big\{ \{1,2\}, \{1,3\}, \{2,3\} \big\}$$

---
### 1.4 $k$-子集 ($k$-subset)
**定义**：大小为 $k$ 的子集。
**记号**：$\binom{[n]}{k}$ 表示 $[n]$ 的所有 $k$-子集构成的集合。
**例子**：$\binom{[3]}{2} = \big\{ \{1,2\}, \{1,3\}, \{2,3\} \big\}$。

---
### 1.5 $k$-均匀族 ($k$-uniform Family)
**定义**：族 $\mathcal{F}$ 中所有集合的大小都等于 $k$。即 $\mathcal{F} \subseteq \binom{[n]}{k}$。
**说明**：课件中所有主要定理（向日葵引理、EKR 定理）讨论的核心对象都是 $k$-均匀族。
**例子**：$\mathcal{F} = \big\{ \{1,2\}, \{1,3\}, \{2,3\} \big\}$ 是 $2$-均匀族。

---
### 1.6 向日葵 (Sunflower)
**定义**：也叫 $\Delta$-系统。一个由 $r$ 个集合构成的族 $\mathcal{G}$，如果其中任意两个集合的交集都相等，则称 $\mathcal{G}$ 是一个大小为 $r$ 的向日葵。这个公共交集称为**花心 (Center)**，可以为空集。
**形式化**：
$$\forall S, T \in \mathcal{G}, S \neq T \Rightarrow S \cap T = C$$
其中 $C$ 是固定的花心。
**例子**：
$$\big\{ \{1,2,3\}, \{1,2,4\}, \{1,2,5\} \big\}$$
是以 $\{1,2\}$ 为花心的向日葵，大小为 $3$。
**特殊情况**：当花心为空集时，向日葵就是 $r$ 个两两不交的集合。

---
### 1.7 相交族 (Intersecting Family)
**定义**：族中任意两个集合的交集都不为空集。
**形式化**：
$$\forall S, T \in \mathcal{F}, S \cap T \neq \emptyset$$
**说明**：当 $n < 2k$ 时，$\binom{[n]}{k}$ 中任意两个 $k$-子集必然相交，因此相交性平凡成立。EKR 定理讨论的是 $n \ge 2k$ 的情形。
**例子**（$n=4, k=2$）：
$$\mathcal{F} = \big\{ \{1,2\}, \{1,3\}, \{1,4\} \big\}$$
任意两个集合都包含元素 $1$，因此是相交族。

---
### 1.8 链 (Chain)
**定义**：族中的集合可以排成一个全序：
$$S_1 \subseteq S_2 \subseteq \dots \subseteq S_m$$
**例子**：
$$\big\{ \{1\}, \{1,2\}, \{1,2,3\} \big\}$$
是一个链。

---
### 1.9 反链 (Antichain)
**定义**：族中没有任何一个集合是另一个集合的子集。
**形式化**：
$$\forall S, T \in \mathcal{F}, S \neq T \Rightarrow S \nsubseteq T \text{ 且 } T \nsubseteq S$$
**说明**：$k$-均匀族自动是反链，因为大小不同的集合不可能相等。但反链可以包含不同大小的集合。
**例子**（$n=3$）：
$$\mathcal{F} = \big\{ \{1,2\}, \{1,3\}, \{2,3\} \big\}$$
任意两个集合大小相同，因此互不包含，是反链。
**例子**（非反链）：
$$\big\{ \{1\}, \{1,2\} \big\}$$
因为 $\{1\} \subseteq \{1,2\}$。

---
### 1.10 上阴影 / 阴影 (Upper Shadow / Shade)
**定义**：对于一个 $k$-均匀族 $\mathcal{F}$，它的上阴影是所有包含 $\mathcal{F}$ 中某个集合的 $(k+1)$-子集构成的族。
**形式化**：
$$\nabla \mathcal{F} = \left\{ T \in \binom{[n]}{k+1} \mid \exists S \in \mathcal{F}, S \subseteq T \right\}$$

**用途**：在 Sperner 定理的证明中，用于将低层集合“提升”一层。
**例子**：设 $\mathcal{F} = \big\{ \{1,2\}, \{1,3\} \big\}$（$n=4, k=2$），则
$$\nabla \mathcal{F} = \big\{ \{1,2,3\}, \{1,2,4\}, \{1,3,4\} \big\}$$

---
### 1.11 下阴影 (Lower Shadow)
**定义**：对于一个 $k$-均匀族 $\mathcal{F}$，它的下阴影是所有被 $\mathcal{F}$ 中某个集合包含的 $(k-1)$-子集构成的族。
**形式化**：
$$\Delta \mathcal{F} = \left\{ T \in \binom{[n]}{k-1} \mid \exists S \in \mathcal{F}, T \subseteq S \right\}$$

**用途**：在 Sperner 定理的证明中，用于将高层集合“下降”一层。
**例子**：设 $\mathcal{F} = \big\{ \{1,2,3\}, \{1,2,4\} \big\}$（$n=4, k=3$），则
$$\Delta \mathcal{F} = \big\{ \{1,2\}, \{1,3\}, \{2,3\}, \{1,4\}, \{2,4\} \big\}$$

---
### 1.12 移位 / 压缩 (Shift / Compression)
**定义**：一个逐步变换族的技术，目的是将族变得更“左倾”（元素编号小的优先），同时保持某些性质（如相交性）。
**操作**：对 $1 \le i < j \le n$，定义 $(i,j)$-移位算子 $S_{ij}$：
$$S_{ij}(\mathcal{F}) = \{ S_{ij}(T) \mid T \in \mathcal{F} \}$$
其中
$$S_{ij}(T) = \begin{cases}
(T \setminus \{j\}) \cup \{i\} & \text{如果 } j \in T,\ i \notin T,\ (T \setminus \{j\}) \cup \{i\} \notin \mathcal{F} \\
T & \text{否则}
\end{cases}$$

**性质**：
1. $|S_{ij}(T)| = |T|$，因此 $|S_{ij}(\mathcal{F})| = |\mathcal{F}|$
2. 如果 $\mathcal{F}$ 是相交族，则 $S_{ij}(\mathcal{F})$ 也是相交族
**用途**：在 EKR 定理的证明中，反复应用移位操作，最终得到一个“移位族”(shifted family)，便于分析。

---
## 2. 向日葵引理
### 2.1 引理陈述
> **Sunflower Lemma (Erdős–Rado, 1960)**
>
> 设 $\mathcal{F} \subseteq \binom{[n]}{k}$。如果
> $$|\mathcal{F}| > k! \cdot (r-1)^k$$
> 那么存在一个子族 $\mathcal{G} \subseteq \mathcal{F}$，使得 $\mathcal{G}$ 是一个大小为 $r$ 的向日葵。

**说明**：
- 这个下界只依赖于 $k$ 和 $r$，而与基集 $[n]$ 的大小无关（只要 $n$ 足够大能容纳这些集合）
- 当 $r=2$ 时，条件变为 $|\mathcal{F}| > k! \cdot 1^k = k!$，这是很自然的结果

---
### 2.2 证明
**证明思路**：使用归纳法（对 $k$ 归纳）。

---
**基础步骤**：$k = 1$
此时 $\mathcal{F} \subseteq \binom{[n]}{1}$，即每个集合是单点。若 $|\mathcal{F}| > r-1$，则存在 $r$ 个不同的单点（它们两两不交，即花心为空集），构成一个大小为 $r$ 的向日葵。

---
**归纳步骤**：$k \ge 2$
假设引理对所有小于 $k$ 的均匀度成立。给定 $\mathcal{F} \subseteq \binom{[n]}{k}$ 满足 $|\mathcal{F}| > k!(r-1)^k$。
**第1步**：构造最大不交子族
取最大的子族 $\mathcal{G} \subseteq \mathcal{F}$，满足 $\mathcal{G}$ 中任意两个集合都不相交。这样的 $\mathcal{G}$ 称为**最大匹配**。
**第2步**：分两种情况讨论
**情况1**：$|\mathcal{G}| \ge r$
此时 $\mathcal{G}$ 本身就是一个向日葵（花心为空集），因为任意两个集合不相交，交为空集。证明完成。
**情况2**：$|\mathcal{G}| \le r-1$
设 $Y = \bigcup_{S \in \mathcal{G}} S$，即 $\mathcal{G}$ 中所有集合的并集。由于每个集合大小为 $k$，且 $\mathcal{G}$ 中的集合两两不交，有：
$$|Y| = \sum_{S \in \mathcal{G}} |S| = k \cdot |\mathcal{G}| \le k(r-1)$$
**第3步**：证明每个集合都与 $Y$ 相交
反证：若存在 $T \in \mathcal{F}$ 使得 $Y \cap T = \emptyset$，则 $\forall S \in \mathcal{G}$，$S \cap T = \emptyset$。于是 $\mathcal{G} \cup \{T\}$ 是更大的不交子族，与 $\mathcal{G}$ 的最大性矛盾。
因此，每个 $S \in \mathcal{F}$ 都与 $Y$ 相交。
**第4步**：找频繁出现的元素
由鸽巢原理，存在某个元素 $x \in Y$，使得包含 $x$ 的集合数量至少为：
$$|\{S \in \mathcal{F} \mid x \in S\}| \ge \frac{|\mathcal{F}|}{|Y|} > \frac{k!(r-1)^k}{k(r-1)} = (k-1)!(r-1)^{k-1}$$
**第5步**：应用归纳假设
定义
$$\mathcal{H} = \{ S \setminus \{x\} \mid S \in \mathcal{F},\ x \in S \}$$
则 $\mathcal{H} \subseteq \binom{[n]}{k-1}$ 是 $(k-1)$-均匀族，且
$$|\mathcal{H}| > (k-1)!(r-1)^{k-1}$$
由归纳假设，$\mathcal{H}$ 包含一个大小为 $r$ 的向日葵 $\mathcal{H}'$。那么
$$\mathcal{G}' = \{ H \cup \{x\} \mid H \in \mathcal{H}' \} \subseteq \mathcal{F}$$
是一个大小为 $r$ 的向日葵（花心为 $\mathcal{H}'$ 的花心并上 $\{x\}$？需要小心）
实际上，若 $\mathcal{H}'$ 的花心为 $C$，则 $\mathcal{G}'$ 中任意两集合的交集为 $C \cup \{x\}$，因此 $\mathcal{G}'$ 是向日葵。
**结论**：无论哪种情况，都存在大小为 $r$ 的向日葵。

---
### 2.3 直观例子
取 $k=2, r=3$，则下界为：
$$2! \cdot (3-1)^2 = 2 \times 4 = 8$$
即：如果一个由对子（大小为2的集合）组成的族，其大小超过 8，那么里面一定藏着一个大小为 3 的向日葵。
**可能的向日葵形式**：
1. **花心为空**：三个两两不交的对子，如 $\{1,2\},\{3,4\},\{5,6\}$
2. **花心大小为 1**：都包含同一个元素，如 $\{1,2\},\{1,3\},\{1,4\}$

---
### 2.4 重要性及应用
**理论意义**：
- 结构强制出现：当族足够大时，一种非常规则的结构（向日葵）会强制出现
- 这是极值集合论中“量变引起质变”的典型例子
**应用领域**：
- 超图理论
- 伪随机性
- 加法组合
- 理论计算机科学（电路复杂性、证明复杂性）

---
### 2.5 向日葵猜想 (Sunflower Conjecture)
**问题**：引理中的上界 $k!(r-1)^k$ 是否是紧的？
**猜想** (Erdős–Rado)：存在一个只依赖于 $r$ 的常数 $C(r)$，使得当 $|\mathcal{F}| > C(r)^k$ 时就存在大小为 $r$ 的向日葵。
**现状**：至今未完全解决，是组合学中的一个著名开放问题。目前已知的最好结果之一为 $C(r) = O(r^3)$。

---
## 3. Erdős–Ko–Rado 定理
### 3.1 定理陈述
> **Erdős–Ko–Rado Theorem (EKR 定理)**
>
> 设 $\mathcal{F} \subseteq \binom{[n]}{k}$ 是相交族，即 $\forall S, T \in \mathcal{F}$，$S \cap T \neq \emptyset$。若 $n \ge 2k$(全集大小至少是子集大小的两倍)，则
> $$|\mathcal{F}| \le \binom{n-1}{k-1}$$

**说明**：
- 当 $n < 2k$ 时，$\binom{[n]}{k}$ 中任意两个 $k$-子集必然相交，因此 $|\mathcal{F}| \le \binom{n}{k}$ 平凡成立
- 达到上界的极值族是“星”(star)：所有包含某个固定元素的 $k$-子集
  $$\mathcal{F} = \{ S \in \binom{[n]}{k} \mid 1 \in S \}$$
  其大小为 $\binom{n-1}{k-1}$
- $n>2k$ 时有唯一的极值结构，只有星一种形式，$n=2k$ 时，可能有多种极值形式
---
### 3.2 证明：归纳法
**证明思路**：对 $n$ 和 $k$ 进行归纳。
**第1步**：分类
固定元素 $n$，将 $\mathcal{F}$ 分为两类：
$$\mathcal{F}_0 = \{ S \in \mathcal{F} \mid n \notin S \}$$
$$\mathcal{F}_1 = \{ S \in \mathcal{F} \mid n \in S \}$$

则 $\mathcal{F} = \mathcal{F}_0 \cup \mathcal{F}_1$ 且 $\mathcal{F}_0 \cap \mathcal{F}_1 = \emptyset$。
**第2步**：处理 $\mathcal{F}_0$
$\mathcal{F}_0 \subseteq \binom{[n-1]}{k}$ 且仍是相交族（因为减少元素不影响相交性）。由归纳假设：
$$|\mathcal{F}_0| \le \binom{n-2}{k-1}$$
**第3步**：处理 $\mathcal{F}_1$
定义
$$\mathcal{F}_1' = \{ S \setminus \{n\} \mid S \in \mathcal{F}_1 \} \subseteq \binom{[n-1]}{k-1}$$

需要证明 $\mathcal{F}_1'$ 是相交族。用反证法：
假设 $\mathcal{F}_1'$ 不是相交族，则存在 $A, B \in \mathcal{F}_1$，使得
$$(A \setminus \{n\}) \cap (B \setminus \{n\}) = \emptyset$$
即 $A \cap B = \{n\}$（因为 $n$ 同时在 $A$ 和 $B$ 中）。
于是 $|A \cup B| = |A| + |B| - |A \cap B| = k + k - 1 = 2k - 1$。
由于 $n \ge 2k$，有 $2k-1 \le n-1$，因此存在某个 $i \notin A \cup B$（且 $i \neq n$）。考虑集合
$$C = (A \setminus \{n\}) \cup \{i\}$$
则 $|C| = k$。由于 $\mathcal{F}$ 是移位族（经过压缩处理后），$C \in \mathcal{F}$。但
$$C \cap B = ((A \setminus \{n\}) \cup \{i\}) \cap B = (A \setminus \{n\}) \cap B = \emptyset$$
因为 $A \setminus \{n\}$ 与 $B \setminus \{n\}$ 不交，且 $i \notin B$。这与 $\mathcal{F}$ 是相交族矛盾。
因此 $\mathcal{F}_1'$ 是相交族。由归纳假设：
$$|\mathcal{F}_1'| \le \binom{n-2}{k-2}$$
而 $|\mathcal{F}_1| = |\mathcal{F}_1'|$，所以 $|\mathcal{F}_1| \le \binom{n-2}{k-2}$。
**第4步**：合并
$$|\mathcal{F}| = |\mathcal{F}_0| + |\mathcal{F}_1| \le \binom{n-2}{k-1} + \binom{n-2}{k-2} = \binom{n-1}{k-1}$$
这里使用了组合恒等式 $\binom{a}{b} + \binom{a}{b-1} = \binom{a+1}{b}$。

---

### 3.3 证明：压缩法 (Shifting)
**核心思想**：通过反复应用移位算子 $S_{ij}$，将任意相交族转化为一个“左倾”的相交族，且大小不变。最终得到的族具有特殊性质，便于分析。
**第1步**：定义移位算子
对 $1 \le i < j \le n$，定义 $(i,j)$-移位：
$$S_{ij}(S) = \begin{cases}
(S \setminus \{j\}) \cup \{i\} & \text{如果 } j \in S,\ i \notin S,\ (S \setminus \{j\}) \cup \{i\} \notin \mathcal{F} \\
S & \text{否则}
\end{cases}$$
$$S_{ij}(\mathcal{F}) = \{ S_{ij}(S) \mid S \in \mathcal{F} \}$$
**第2步**：性质
1. $|S_{ij}(\mathcal{F})| = |\mathcal{F}|$
2. 若 $\mathcal{F}$ 是相交族，则 $S_{ij}(\mathcal{F})$ 也是相交族
**第3步**：反复压缩
反复对所有 $i<j$ 应用移位算子，最终得到一个**移位族**(shifted family) $\mathcal{F}^*$，满足：
- $|\mathcal{F}^*| = |\mathcal{F}|$
- $\mathcal{F}^*$ 仍是相交族
- $\mathcal{F}^*$ 具有“左倾”性质：若 $S \in \mathcal{F}^*$ 且 $i<j$，则交换元素后得到的集合也在 $\mathcal{F}^*$ 中（在某种意义下）
**第4步**：分析移位族
对于移位族 $\mathcal{F}^*$，可以证明它的极值结构就是所有包含元素 $1$ 的 $k$-子集。因此
$$|\mathcal{F}^*| \le \binom{n-1}{k-1}$$
从而原族 $\mathcal{F}$ 满足同样的上界。

---
### 3.4 证明：Katona 的双计数法
这是 EKR 定理最优雅的证明之一。
**第1步**：循环排列
考虑 $[n]$ 的一个循环排列 $\pi = (\pi_1, \pi_2, \dots, \pi_n)$，其中下标模 $n$ 理解。共有 $(n-1)!$ 个不同的循环排列。
**第2步**：$k$-弧
对每个 $i \in [n]$，定义
$$A_i = \{ \pi_{i+1}, \pi_{i+2}, \dots, \pi_{i+k} \}$$
即从位置 $i+1$ 开始的连续 $k$ 个元素（模 $n$ 理解）。记
$$\mathcal{G}_\pi = \{ A_1, A_2, \dots, A_n \}$$
**第3步**：引理
> **引理**：当 $n \ge 2k$ 时，对任意循环排列 $\pi$，有 $|\mathcal{G}_\pi \cap \mathcal{F}| \le k$。

**证明**：
- $A_i$ 和 $A_{i+k}$ 是不交的（因为中间隔了 $k$ 个位置，总长为 $2k \le n$），因此 $\mathcal{F}$ 不能同时包含它们
- 这 $n$ 个集合可以配成 $k$ 对：$(A_1, A_{1+k}), (A_2, A_{2+k}), \dots, (A_k, A_{2k})$
- 每对中至多选一个，所以最多选 $k$ 个
- 实际上可以达到 $k$：比如选 $A_1, A_2, \dots, A_k$（它们两两相交？需要验证）
**第4步**：双计数
定义
$$R = \{ (S, \pi) \mid \pi \text{ 是循环排列}, S \in \mathcal{G}_\pi \cap \mathcal{F} \}$$
**方法1**：固定 $\pi$，由引理，$|\mathcal{G}_\pi \cap \mathcal{F}| \le k$，所以
$$|R| \le (n-1)! \cdot k$$

**方法2**：固定 $S \in \mathcal{F}$，数一数有多少个循环排列 $\pi$ 使得 $S \in \mathcal{G}_\pi$。
$S$ 在循环排列中作为连续 $k$ 个元素出现的方式：
- 首先，$S$ 中 $k$ 个元素的相对顺序有 $k!$ 种
- 其次，$[n] \setminus S$ 中 $n-k$ 个元素的相对顺序有 $(n-k)!$ 种
- 然后，将这两段拼接成一个循环排列：需要决定 $S$ 的起始位置（在循环中固定），这有 $1$ 种方式（因为循环排列已经模旋转等价）
实际上，标准计算为：每个 $S$ 对应 $k!(n-k)!$ 个不同的循环排列。
因此
$$|R| = \sum_{S \in \mathcal{F}} k!(n-k)! = |\mathcal{F}| \cdot k!(n-k)!$$
**第5步**：联立不等式
$$|\mathcal{F}| \cdot k!(n-k)! \le (n-1)! \cdot k$$
整理得
$$|\mathcal{F}| \le \frac{(n-1)! \cdot k}{k!(n-k)!} = \frac{(n-1)!}{(k-1)!(n-k)!} = \binom{n-1}{k-1}$$
---
### 3.5 极值结构
**定理**：当 $n > 2k$ 时，达到上界 $\binom{n-1}{k-1}$ 的相交族只能是“星”(star)，即所有包含某个固定元素的 $k$-子集。
**说明**：当 $n = 2k$ 时，还存在其他极值结构。例如，$n=4, k=2$ 时，$\binom{4}{2} = 6$，上界为 $\binom{3}{1}=3$。除了星 $\{\{1,2\},\{1,3\},\{1,4\}\}$，还有 $\{\{1,2\},\{1,3\},\{2,3\}\}$？检查：$\{1,3\} \cap \{2,3\} = \{3\} \neq \emptyset$，但 $\{1,2\} \cap \{2,3\} = \{2\}$，实际上这个族确实相交？不，$\{1,2\}$ 和 $\{3,4\}$ 不可同时存在... 需要仔细验证。实际上 $n=2k$ 时还存在另一种极值族：所有大小恰好为 $k$ 且包含某个固定元素的子集，或者所有大小恰好为 $k$ 且包含某两个元素之一的子集？经典结果是：当 $n=2k$ 时，极值族可以是所有大小恰好为 $k$ 且经过某个固定 $k$-子集的子集... 更准确地说：当 $n=2k$ 时，有两种极值结构：
1. 星（所有包含固定元素的 $k$-子集）
2. 补集为星的族（即所有包含在某个固定 $k$-子集的补集中的 $k$-子集）

---
## 4. Sperner 定理
### 4.1 定理陈述
> **Sperner Theorem (Sperner, 1928)**
> 设 $\mathcal{F} \subseteq 2^{[n]}$ 是反链（即任意两个集合互不包含），则
> $$|\mathcal{F}| \le \binom{n}{\lfloor n/2 \rfloor}$$

**说明**：
- 达到上界的**反链是“中间层”**：所有大小为 $\lfloor n/2 \rfloor$ 的子集
- 当 $n$ 为偶数时，中间层唯一；当 $n$ 为奇数时，还有大小为 $\lceil n/2 \rceil$ 的层也达到最大值
**例子**：
- $n=3$：最大反链大小为 $\binom{3}{1}=3$，例如 $\{\{1\},\{2\},\{3\}\}$ 或 $\{\{1,2\},\{1,3\},\{2,3\}\}$
- $n=4$：最大反链大小为 $\binom{4}{2}=6$，即所有大小为2的子集

---
### 4.2 证明：阴影法
**第1步**：引理（Sperner 引理）
> **引理**：设 $\mathcal{F} \subseteq \binom{[n]}{k}$ 是 $k$-均匀族，则
> $$|\nabla \mathcal{F}| \ge \frac{n-k}{k+1} |\mathcal{F}|$$
> $$|\Delta \mathcal{F}| \ge \frac{k}{n-k+1} |\mathcal{F}|$$

**证明**（双计数法）：
考虑 $R = \{ (S, T) \mid S \in \mathcal{F}, T \in \nabla \mathcal{F}, S \subseteq T \}$。
- 方法1：固定 $S \in \mathcal{F}$，有多少个 $T$ 包含 $S$？需要从 $[n] \setminus S$ 中选一个元素加入，有 $n-k$ 种。所以 $|R| = |\mathcal{F}| \cdot (n-k)$。
- 方法2：固定 $T \in \nabla \mathcal{F}$，有多少个 $S \in \mathcal{F}$ 包含在 $T$ 中？$T$ 的大小为 $k+1$，它的 $k$-子集有 $k+1$ 个，但其中可能在 $\mathcal{F}$ 中的不超过 $k+1$。所以 $|R| \le |\nabla \mathcal{F}| \cdot (k+1)$。
联立得 $|\nabla \mathcal{F}| \ge \frac{n-k}{k+1} |\mathcal{F}|$。
类似地可证第二个不等式。
**第2步**：证明 Sperner 定理
设 $\mathcal{F}$ 是反链。令 $\mathcal{F}_k = \mathcal{F} \cap \binom{[n]}{k}$。
令 $k_{\min}$ 为使 $|\mathcal{F}_k| > 0$ 的最小的 $k$，$k_{\max}$ 为使 $|\mathcal{F}_k| > 0$ 的最大的 $k$。
**情况1**：$k_{\min} \le \frac{n-1}{2}$。将 $\mathcal{F}_{k_{\min}}$ 替换为 $\nabla \mathcal{F}_{k_{\min}}$。
由引理，$|\nabla \mathcal{F}_{k_{\min}}| \ge \frac{n-k_{\min}}{k_{\min}+1} |\mathcal{F}_{k_{\min}}| \ge |\mathcal{F}_{k_{\min}}|$（因为 $k_{\min} \le \frac{n-1}{2}$ 意味着 $n-k_{\min} \ge k_{\min}+1$）。
**情况2**：$k_{\max} \ge \frac{n+1}{2}$。将 $\mathcal{F}_{k_{\max}}$ 替换为 $\Delta \mathcal{F}_{k_{\max}}$。
由引理，$|\Delta \mathcal{F}_{k_{\max}}| \ge \frac{k_{\max}}{n-k_{\max}+1} |\mathcal{F}_{k_{\max}}| \ge |\mathcal{F}_{k_{\max}}|$。
重复这个过程，直到所有非空层都向中间靠拢，最终得到一个均匀族（所有集合大小相同）且大小不减。由于最终族是反链（可证），它必须是某一层的全体，因此大小不超过 $\binom{n}{\lfloor n/2 \rfloor}$。

---
## 5. LYM 不等式
### 5.1 不等式陈述
> **LYM Inequality (Lubell–Yamamoto–Meshalkin)**
>
> 设 $\mathcal{F} \subseteq 2^{[n]}$ 是反链，则
> $$\sum_{S \in \mathcal{F}} \frac{1}{\binom{n}{|S|}} \le 1$$

**说明**：LYM 不等式比 Sperner 定理更强。事实上，Sperner 定理是其直接推论。
**推导 Sperner 定理**：
$$\sum_{S \in \mathcal{F}} \frac{1}{\binom{n}{|S|}} \ge |\mathcal{F}| \cdot \frac{1}{\binom{n}{\lfloor n/2 \rfloor}}$$
结合 LYM 不等式得 $|\mathcal{F}| \le \binom{n}{\lfloor n/2 \rfloor}$。

---
### 5.2 证明：概率方法
这是 LYM 不等式最简洁的证明之一（Alon 的证明）。
**第1步**：随机排列
随机均匀选取 $[n]$ 的一个排列 $\pi = (\pi_1, \pi_2, \dots, \pi_n)$。共有 $n!$ 种等可能的排列。
**第2步**：定义链
对每个排列 $\pi$，定义最大链
$$\mathcal{C}_\pi = \{ \{\pi_1\}, \{\pi_1, \pi_2\}, \dots, \{\pi_1, \pi_2, \dots, \pi_n\} \}$$
即 $\pi$ 的前缀构成的集合链。
**第3步**：指示变量
对每个 $S \in \mathcal{F}$，定义指示变量
$$\chi_S = \mathbf{1}\{ S \in \mathcal{C}_\pi \}$$
即 $\chi_S = 1$ 当且仅当 $S$ 恰好是 $\pi$ 的前 $|S|$ 个元素构成的集合。
**第4步**：计算期望
对于给定的 $S \in \mathcal{F}$，$S \in \mathcal{C}_\pi$ 当且仅当 $\pi$ 的前 $|S|$ 个元素恰好是 $S$ 中的元素（顺序任意）。因此($Pr$ 为 $Probability$ 缩写，表示概率)
$$\Pr(S \in \mathcal{C}_\pi) = \frac{|S|!(n-|S|)!}{n!} = \frac{1}{\binom{n}{|S|}}$$
所以
$$\mathbb{E}[\chi_S] = \frac{1}{\binom{n}{|S|}}$$
**第5步**：定义 $X$
令 $X = \sum_{S \in \mathcal{F}} \chi_S$。则 $X$ 是 $\mathcal{C}_\pi$ 中属于 $\mathcal{F}$ 的集合个数。
由于 $\mathcal{F}$ 是反链，而 $\mathcal{C}_\pi$ 是链，一条链和一个反链的交集最多包含 $1$ 个元素。因此 $X \le 1$ 对所有 $\pi$ 成立。
于是
$$\mathbb{E}[X] \le 1$$
**第6步**：联立
但
$$\mathbb{E}[X] = \sum_{S \in \mathcal{F}} \mathbb{E}[\chi_S] = \sum_{S \in \mathcal{F}} \frac{1}{\binom{n}{|S|}}$$

因此
$$\sum_{S \in \mathcal{F}} \frac{1}{\binom{n}{|S|}} \le 1$$
---
### 5.3 LYM 不等式的推广
LYM 不等式有多种推广形式，例如：
- **Bollobás 不等式**：对于两个集合族 $\mathcal{A}, \mathcal{B}$ 满足 $A_i \cap B_i = \emptyset$ 且 $A_i \cap B_j \neq \emptyset$（$i \neq j$），有 $\sum_i 1/\binom{|A_i|+|B_i|}{|A_i|} \le 1$
- **Skew 版本**
- **多重集版本**
---
## 总结表

| 概念/定理      | 英文                          | 核心结论                                            |
| :--------- | :-------------------------- | :---------------------------------------------- |
| $k$-均匀族    | $k$-uniform family          | 所有集合大小都是 $k$                                    |
| 向日葵        | Sunflower / $\Delta$-system | 两两交集相等                                          |
| 相交族        | Intersecting family         | 任意两集合有交                                         |
| 反链         | Antichain                   | 无包含关系                                           |
| 向日葵引理      | Sunflower Lemma             | $\mathcal{F}> k!(r-1)^k \Rightarrow$ 存在 $r$-向日葵 |
| EKR 定理     | Erdős–Ko–Rado Theorem       | 相交族最大为 $\binom{n-1}{k-1}$                       |
| Sperner 定理 | Sperner Theorem             | 反链最大为 $\binom{n}{\lfloor n/2 \rfloor}$          |
| LYM 不等式    | LYM Inequality              | $\sum_{S \in \mathcal{F}} 1/\binom{n}{S} \le 1$ |

---
## 常见证明技巧总结
1. **双计数法 (Double Counting)**：从两个角度计算同一对象，导出不等式
2. **归纳法 (Induction)**：对 $n$ 或 $k$ 进行归纳
3. **压缩/移位法 (Compression/Shifting)**：逐步变换族使其更规则
4. **概率方法 (Probabilistic Method)**：引入随机性，用期望推导不等式
5. **极值结构刻画**：找出达到极值的族的结构特征
---
## 公开问题
1. **向日葵猜想**：是否存在常数 $C(r)$ 使得 $|\mathcal{F}| > C(r)^k$ 保证存在 $r$-向日葵？
2. **EKR 定理的推广**：对于 $n$ 稍大于 $2k$ 时，极值结构是否唯一？
3. **Sperner 定理的推广**：对于更一般的偏序集，最大反链的大小如何？